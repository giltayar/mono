'use strict'
const {describe = global.describe, it = global.it} = require('mocha')
const {expect} = require('chai')

describe('templatetemplate', function() {
  it('should do amazing stuff', async () => {
    expect(4).to.equal(4)
  })
})
